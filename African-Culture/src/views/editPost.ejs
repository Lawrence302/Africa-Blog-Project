<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css"
      integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/css/styles.css" />
    <link rel="stylesheet" href="/css/createPost.css" />
    <title>Document</title>
  </head>
  <body class="bg-white">
    <div class="container bg-white mt-4">
      <!-- <% if(!article){ %> <% }else{ %> <%= article %> <% } %> -->
      <form action="/articles/edit/<%=article.id%>?_method=PUT" method="POST">
        <div class="form-group">
          <label for="title">Title:</label>
          <input
            type="text"
            name="title"
            value="<%= article && article.title  %>"
            class="form-control"
            placeholder="Article Title"
          />
        </div>
        <div class="form-group">
          <label for="title">Category:</label>
          <select class="form-control" id="category" name="category">
            <option value="dance" <%= article && article.category === "dance" ? "selected" : "" %> >Dance</option>
            <option value="culture" <%= article && article.category === "culture" ? "selected" : "" %>>Culture</option>
            <option value="music" <%= article && article.category === "music" ? "selected" : "" %>>Music</option>
            <option value="climate" <%= article && article.category === "climate" ? "selected" : "" %>>Climate</option>
            <option value="love" <%= article && article.category === "love" ? "selected" : "" %>>Love</option>
          </select>
        </div>

        <div class="form-group">
          <label for="title">Featured:</label>
          <select class="form-control" id="category" name="featured">
            <option value=0 <%= article && article.featured === 0 ? "selected" : "" %> >False</option>
            <option value=1 <%= article && article.featured === 1 ? "selected" : "" %>>True</option>
             </select>
        </div>

        <div class="form-group">
          <label for="title">Slug:</label>
          <input
            type="text"
            name="slug"
            class="form-control"
            value="<%= article && article.slug  %>"
            placeholder="example : culture-of-cameroon"
          />
        </div>

        <div class="form-group">
          <label for="description">Description:</label>
          <textarea
            type="text"
            class="form-control"
            placeholder="Article description or Summary"
            name="description"
            rows="5"
          >
          <%= article && article.summary  %>
          </textarea>
        </div>
        <div class="form-group">
          <label for="image">Image Link : </label>
          <input
            type="url"
            class="form-control"
            value="<%= article && article.image_url  %>"
            placeholder="www.example.com/image-name"
            name="image"
          />
        </div>
        <div class="form-group">
          <label for="keywords">Keywords:</label>
          <input
            type="text"
            class="form-control"
            value="<%= article && article.meta_keywords  %>"
            placeholder="Enter keywords seperated by comma"
            name="keywords"
          />
        </div>
        <div class="form-group">
          <label for="content">Content:</label>
          <textarea
            type="text"
            class="form-control"
            placeholder="Article Content using markdown"
            name="content"
            rows="30"
          >
          <%= article && article.content %>
        </textarea
          >
        </div>
        <div>
          <% if(!response || response == null){ %> <% }else{ %> <%=
          response.failed %> <% } %>
        </div>
        <a href="/">
          <button type="button" class="btn btn-secondary">Cancel</button>
        </a>
        
        <button type="submit" class="btn btn-primary">Save</button>
      </form>
    </div>
  </body>
</html>
