<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@300;400&family=Roboto:ital,wght@0,300;0,400;1,100&display=swap"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/css/register.css" />
    <title>Document</title>
  </head>
  <body>
    <div class="container">
      <% if ( !response || response == null || response == undefined ){ %> <%
      }else { %> <% if(response.signal) { %> <% console.log(response) %>
      <div class="pass-result-div">
        <p><%= response.message %></p>
      </div>
      <% } else {%>
      <div class="fail-result-div">
        <p><%= response.message %></p>
      </div>
      <%} %> <%} %>

      <form action="/author/login" method="POST">
        <label for="email">Email:</label>
        <input type="email" id="email" name="email" required />
        <br />
        <label for="password">Password:</label>
        <input type="password" id="password" name="password" required />
        <br />
        <input type="submit" class="submit_btn" value="Login" />
        <br />
        <div class="not-signed">
          <p>not registered ? <a href="/author/register">Register here</a></p>
        </div>
      </form>
    </div>
  </body>
</html>
